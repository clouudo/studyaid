RewriteEngine On
SetEnv TMPDIR /Applications/XAMPP/xamppfiles/htdocs/studyaid/temp
# Adjust if your app is in a subfolder (here: /studyaid/)
RewriteBase /studyaid/

# Skip existing files and directories
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Route everything else to index.php as url=...
RewriteRule ^(.+)$ index.php?url=$1 [QSA,L]

Options -Indexes
RewriteEngine On
RewriteBase /studyaid/

RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ index.php?url=$1 [L,QSA]